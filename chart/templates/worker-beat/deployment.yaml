{{- if and .Values.worker.enabled .Values.worker.beat.enabled }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "banjo.fullname" . }}-worker-beat
  {{- with (include "banjo.appDefaultDeploymentAnnotations" $) -}}{{ . | nindent 2 }}{{- end }}
  labels:
    app: {{ include "banjo.fullname" . }}
    component: worker-beat
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
spec:
  replicas: 1  # This should only 1
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
      app: {{ include "banjo.fullname" . }}
      component: worker-beat
  template:
    metadata:
      annotations:
        {{- include "banjo.appDefaultAnnotations" $ | nindent 8 }}
      labels:
        app: {{ include "banjo.fullname" . }}
        component: worker-beat
        {{- with (include "banjo.appDefaultLabels" $) -}}{{ . | nindent 8 }}{{- end }}
    spec:
      {{- with (include "banjo.appDefaultVolumes" $) -}}{{ . | nindent 6 }}{{- end }}
      {{- if .Values.serviceAccount.create }}
      serviceAccountName: {{ include "banjo.serviceAccountName" $ }}
      {{- end }}
      containers:
        - name: worker-beat
          {{- include "banjo.imageConfig"
              (dict "Default" $.Values.image "Override" $.Values.worker.beat.image)
              | nindent 10
          }}
          command:
            {{- toYaml .Values.worker.beat.command | nindent 12 }}
          # TODO: livenessProbe
          resources:
            {{- toYaml .Values.worker.beat.resources | nindent 12 }}
          envFrom:
            {{- include "banjo.envFromTemplate" $ | nindent 12 }}
          env:
            {{- include "banjo.envTemplate"
                (dict "Values" $.Values "Type" "worker" )
                | nindent 12
            }}
          {{- with (include "banjo.appDefaultVolumeMounts" $) -}}{{ . | nindent 10 }}{{- end }}

{{- end }}
